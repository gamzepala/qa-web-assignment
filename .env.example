# Copy to .env and adjust if you need to point the suite somewhere other than
# the local preview server. Everything here has a working default, so a clean
# clone runs without creating this file at all.

# Where the app under test is served.
# Leave unset to use the local production preview that Playwright starts itself.
BASE_URL=http://127.0.0.1:4173
